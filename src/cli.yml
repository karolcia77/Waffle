name: WAFFLE
version: "0.0.1a"
author: iy <github.com/Nyanguy>
about: It's a stack-based VM
args:
  - binary:
      short: b
      long: binary
      value_name: BINARY
      help: Sets the mode for the file execution. If set, it will try to execute the byte-code directly.
  - FILE:
      help: Sets the source file to use.
      required: true
      index: 1